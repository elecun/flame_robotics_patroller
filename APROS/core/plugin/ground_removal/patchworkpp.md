 파라미터명      | 기본값 | 단위 / 범위         | 설명
  -----------------|--------|---------------------|---------------------------------------------------------------------------------------
   min_r           | 0.5    | m (미터)            | 최소 반경 거리를 설정합니다. 로봇 본체 근처 노이즈나 로봇 자체 구조물이 지면으로
                   |        |                     | 분류되는 것을 방지하기 위한 최소 검서 반경입니다.
   max_r           | 80.0   | m (미터)            | 최대 탐지 반경 거리입니다. 이 반경을 벗어난 외곽 포인트들은 Ground Removal 계산
                   |        |                     | 대상에서 제외됩니다.
   th_dist         | 0.12   | m (미터)            | 지면 평면과의 거리 임계값 (Inlier Threshold)입니다. 추정된 지면 평면으로부터 점까지의
                   |        |                     | 거리가 이 값보다 작은 포인트를 지면(Ground)으로 최종 분류합니다. (값이 클수록 더 넓은
                   |        |                     | 오차범위의 지면 포인트를 지면으로 간주)
   th_seeds        | 0.4    | m (미터)            | 시드 포인트(Seed Points) 임계높이입니다. 영역 내 최하위 포인트(LPR) 높이 기준으로
                   |        |                     | th_seeds 이내 높이에 있는 포인트들을 지면 평면 피팅(PCA/SVD)을 위한 초기 시드 점으로
                   |        |                     | 사용합니다.
   czm_num_zones   | 4      | 개수                | CZM(Concentric Zone Model) 동심원 구역 분할 수입니다. 반경(min_r ~ max_r)을 몇 개의
                   |        |                     | Ring 구간으로 분할하여 개별 지면 추정을 수행할지 결정합니다.
   num_sectors     | 16     | 개수                | 각 CZM 구역당 방위각(Azimuth) 부채꼴 분할 수입니다. 360도를 16개 섹터(각 섹터당
                   |        |                     | 22.5도)로 나누어 영 영역별 독립 평면 추정(RGPF)을 진행합니다.
   max_iter        | 3      | 회                  | 영역별 지면 평면 피팅(PCA) 최대 반복 횟수입니다.
   num_lpr         | 20     | 개수                | LPR (Lowest Point Representative) 샘플링 개수입니다. 각 섹터 내에서 가장 낮은 z값을
                   |        |                     | 가진 상위 20개 포인트를 뽑아 평균 높이를 구한 뒤 시드점 선택의 기준점으로 활용합니다.
   uprightness_thr | 0.707  | Cosine 값 (cos 45°) | 지면 법선 벡터(Normal Vector)의 수직성 임계값입니다. 추정된 평면의 법선 vector의 Z축
                   |        |                     | 성분이 0.707(경사각 약 45도 이내) 이상일 때만 유효한 지면 평면으로 승인합니다.